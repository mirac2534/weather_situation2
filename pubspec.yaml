name: hava_durumu
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.4.0
  flutter_analog_clock: ^1.0.3
  intl: ^0.18.0
  lottie: ^3.1.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.2
  dio: ^5.7.0
  geocoding: ^3.0.0
  flutter_launcher_icons: ^0.14.3
  connectivity_plus: ^6.1.4
  fl_chart: ^0.66.2
  flutter_local_notifications: ^14.1.5
  awesome_notifications: ^0.8.0
  timezone: ^0.9.2
  android_intent_plus: 4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/models/cities.json
    - assets/animations/

flutter_icons:
  android: true
  ios: true
  image_path: "lib/logo.jpeg"
